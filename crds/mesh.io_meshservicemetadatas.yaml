---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (unknown)
  name: meshservicemetadatas.mesh.io
spec:
  group: mesh.io
  names:
    kind: MeshServiceMetadata
    listKind: MeshServiceMetadataList
    plural: meshservicemetadatas
    shortNames:
    - msm
    singular: meshservicemetadata
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MeshServiceMetadata is the Schema for the meshservicemetadata
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MeshServiceMetadataSpec defines the desired state of MeshServiceMetadata
            properties:
              ownedResources:
                items:
                  properties:
                    apiVersion:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                    stale:
                      type: boolean
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - apiVersion
                  - kind
                  - name
                  - namespace
                  - uid
                  type: object
                type: array
              owners:
                items:
                  properties:
                    apiVersion:
                      type: string
                    cluster:
                      type: string
                    kind:
                      type: string
                    name:
                      type: string
                    uid:
                      description: |-
                        UID is a type that holds unique ID values, including UUIDs.  Because we
                        don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                        intent and helps make sure that UIDs and names do not get conflated.
                      type: string
                  required:
                  - apiVersion
                  - cluster
                  - kind
                  - name
                  - uid
                  type: object
                type: array
            required:
            - ownedResources
            - owners
            type: object
          status:
            description: MeshServiceMetadataStatus defines the observed state of MeshServiceMetadata
            properties:
              lastReconciledTime:
                format: date-time
                type: string
              reconciledObjectHash:
                additionalProperties:
                  type: string
                type: object
              reconciledTemplateHash:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
