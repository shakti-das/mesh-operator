apiVersion: v1
kind: Service
metadata:
  name: orders
  namespace: demo
  labels:
    app: orders
    routing.mesh.io/enabled: "true"
spec:
  ports:
    - name: http
      port: 8080
      protocol: TCP
      targetPort: 8080
  selector:
    app: orders
  type: ClusterIP
