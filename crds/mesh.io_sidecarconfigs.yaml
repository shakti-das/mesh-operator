---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: (unknown)
  name: sidecarconfigs.mesh.io
spec:
  group: mesh.io
  names:
    kind: SidecarConfig
    listKind: SidecarConfigList
    plural: sidecarconfigs
    shortNames:
    - scc
    singular: sidecarconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SidecarConfig is the Schema for the sidecarconfigs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SidecarConfigSpec defines the desired state of SidecarConfig
            properties:
              egressHosts:
                description: |-
                  EgressHosts configures the egress hosts that sidecars can communicate with.
                  This controls which external services the sidecar proxy can route outbound traffic to.
                properties:
                  discovered:
                    description: |-
                      Discovered contains hosts that are automatically discovered and updated by controllers.
                      These should not be manually configured as they are managed by the system.
                    properties:
                      config:
                        description: |-
                          Config contains hosts discovered from configuration sources such as SBOMs
                          (Software Bill of Materials) and other declarative configuration artifacts.
                        items:
                          description: Host defines a destination host for egress
                            traffic
                          properties:
                            hostname:
                              description: |-
                                Hostname specifies the DNS name or IP address of the destination host.
                                This is the primary identifier for the egress destination.
                              type: string
                            namespace:
                              description: |-
                                Namespace specifies the namespace of the service (if applicable).
                                This is typically used for internal service references within the cluster.
                              type: string
                            port:
                              description: |-
                                Port specifies the port number to access on the destination host.
                                If not specified, the default port for the protocol is used.
                              format: int32
                              type: integer
                          required:
                          - hostname
                          type: object
                        type: array
                      runtime:
                        description: |-
                          Runtime contains hosts dynamically discovered based on runtime observations such as
                          traffic patterns, metrics, events, and other behavioral data.
                        items:
                          description: Host defines a destination host for egress
                            traffic
                          properties:
                            hostname:
                              description: |-
                                Hostname specifies the DNS name or IP address of the destination host.
                                This is the primary identifier for the egress destination.
                              type: string
                            namespace:
                              description: |-
                                Namespace specifies the namespace of the service (if applicable).
                                This is typically used for internal service references within the cluster.
                              type: string
                            port:
                              description: |-
                                Port specifies the port number to access on the destination host.
                                If not specified, the default port for the protocol is used.
                              format: int32
                              type: integer
                          required:
                          - hostname
                          type: object
                        type: array
                    type: object
                type: object
              workloadSelector:
                description: |-
                  WorkloadSelector specifies the criteria used to select workloads that this configuration applies to.
                  If defined in the mesh control plane namespace, it applies to all workloads across all namespaces.
                  If not specified, the configuration applies to all workloads in the same namespace and gets merged
                  with any other applicable configurations for the workload or namespace.
                properties:
                  labels:
                    additionalProperties:
                      type: string
                    description: |-
                      Labels specifies a set of key-value pairs that must match the workload labels for this configuration to apply.
                      All specified labels must match for a workload to be selected.
                    type: object
                type: object
            type: object
          status:
            description: SidecarConfigStatus defines the observed state of SidecarConfig
            properties:
              lastReconciledTime:
                description: LastReconciledTime is the timestamp of the last successful
                  reconciliation
                format: date-time
                type: string
              message:
                description: Message provides additional information about the current
                  phase
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed SidecarConfig
                format: int64
                type: integer
              phase:
                description: Phase represents the current state of the SidecarConfig
                  reconciliation
                enum:
                - Pending
                - Succeeded
                - Failed
                - Terminating
                - Unknown
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
